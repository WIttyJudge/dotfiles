#!/bin/sh

function get_brightness() {
  brightnessctl info | sed -En 's/.*\(([0-9]+)%\).*/\1/p'
}

brightness="$(get_brightness)"

icon="🔆"
[ $brightness -le 69 ] && icon="🔅"

printf "%s %s" "$icon" "$brightness%"
