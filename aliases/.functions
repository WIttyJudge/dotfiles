function test() {
    echo "test1"
    echo "test2"
}
    
function acp() {
    git add . 
    git commit -m "Auto Commit"
    git push origin master
}

t-micro() {
  arecord -vv -f dat /dev/null
}

t-sound() {
  speaker-test -c2 -t wav
}

t-internet() {
  ping -c 3 8.8.8.8
}
